# Twinkle-Image 本地启动与镜像打包教程

## 一、统一端口和数据存储

| 用途 | 地址 |
| --- | --- |
| 前端开发 | `http://127.0.0.1:46311` |
| 后端开发 | `http://127.0.0.1:46312` |
| Docker 一体化服务 | `http://127.0.0.1:46311` |

本项目任务队列使用 `better-sqlite3`，数据库文件为 `data\nova-tasks.sqlite`。当前代码不支持 PostgreSQL、MySQL 或 Redis，因此不能直接填写 `E:\docker` 的连接信息；Docker 运行时通过挂载项目 `data` 目录持久化 SQLite 和生成图片。不要强行改为其他数据库，否则需要完整的数据访问层迁移和回归测试。

## 二、安装依赖

```powershell
Set-Location C:\Users\14740\Desktop\Twinkle-Project\Twinkle-Image
npm install
npm --prefix frontend install
npm --prefix backend install
```

API Key、模型和上游地址在前端设置页维护，不要写入镜像。

## 三、终端分别启动前后端

终端一，启动后端：

```powershell
Set-Location C:\Users\14740\Desktop\Twinkle-Project\Twinkle-Image
$env:NODE_ENV = "production"
$env:HOSTNAME = "127.0.0.1"
$env:PORT = "46312"
$env:NOVA_TASK_DB = "$pwd\data\nova-tasks.sqlite"
$env:NOVA_IMAGE_DIR = "$pwd\data\nova-images"
node backend\server.js
```

终端二，启动前端：

```powershell
Set-Location C:\Users\14740\Desktop\Twinkle-Project\Twinkle-Image\frontend
$env:NODE_ENV = "development"
$env:NOVA_BACKEND_URL = "http://127.0.0.1:46312"
$env:NEXT_PUBLIC_BACKEND_URL = "http://127.0.0.1:46312"
npm run dev -- --hostname 127.0.0.1 --port 46311
```

浏览器访问：<http://127.0.0.1:46311>

也可在根目录一键启动后台进程：

```powershell
.\scripts\start-local-dev.ps1
```

脚本会打印两个 PID 并把日志写到项目根目录。停止进程：

```powershell
Stop-Process -Id <前端PID>,<后端PID>
```

## 四、生产构建检查

```powershell
npm run build
$env:PORT = "46311"
$env:HOSTNAME = "127.0.0.1"
npm start
```

此模式由 Node 后端在同一端口提供前端静态页面和 API。

## 五、使用 deploy 脚本构建最新 Docker 镜像

```powershell
Set-Location C:\Users\14740\Desktop\Twinkle-Project\Twinkle-Image
powershell -ExecutionPolicy Bypass -File .\deploy\build-image.ps1 -NoVersionBump
```

当前版本为 `3.1.11`。脚本会构建 `twinkle-image:3.1.11`，并在 `deploy` 目录导出 tar 与 SHA256 校验文件。若要开始下一个补丁版本并同步项目中的版本字段，省略 `-NoVersionBump`：

```powershell
powershell -ExecutionPolicy Bypass -File .\deploy\build-image.ps1
```

## 六、运行镜像并持久化数据

推荐使用 Compose：

```powershell
docker compose up -d
docker compose ps
docker compose logs -f twinkle-image
```

访问：<http://127.0.0.1:46311>

Compose 将宿主机 `data` 挂载到容器，重新构建镜像不会删除 SQLite 数据和生成图片。停止服务时不要删除 `data`：

```powershell
docker compose down
```

## 七、导出、导入和推送镜像

```powershell
docker load -i .\deploy\twinkle-image-3.1.11.tar
docker tag twinkle-image:3.1.11 <你的仓库>/twinkle-image:3.1.11
docker push <你的仓库>/twinkle-image:3.1.11
```

只导出镜像不会包含 `data` 目录；上线迁移时必须单独备份 `data\nova-tasks.sqlite` 和 `data\nova-images`。
